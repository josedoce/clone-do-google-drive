{
  "name": "gdrive-webapi",
  "version": "1.0.0",
  "description": "um clone do gdrive feito na semana javascript",
  "main": "index.js",
  "type": "module",
  "scripts": {
    "test": "cross-env NODE_OPTIONS=--experimental-vm-modules npx jest --runInBand",
    "test:watch": "cross-env NODE_OPTIONS=--experimental-vm-modules npx jest --watch --runInBand",
    "test:cov": "cross-env NODE_OPTIONS=--experimental-vm-modules npx jest --no-cache --runInBand --coverage",
    "start": "mkdir downloads & node src/index.js"
  },
  "author": "josedoce",
  "license": "ISC",
  "devDependencies": {
    "jest": "^27.1.0",
    "nodemon": "^2.0.12"
  },
  "dependencies": {
    "cross-env": "^7.0.3",
    "pino": "^6.8.0",
    "pino-pretty": "^5.1.3",
    "pretty-bytes": "^5.6.0",
    "socket.io": "^4.1.3"
  }
}
